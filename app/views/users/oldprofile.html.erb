<div class="container mt-4">
  <div class="row">
    <div class="col-md-8 offset-md-2">
      <div class="card">
        <div class="card-body">
          <h2 class="card-title"><%= @user.name %></h2>
          <p class="card-text"><strong>Email:</strong> <%= @user.email %></p>
          <p class="card-text"><strong>Member since:</strong> <%= @user.created_at.strftime("%B %d, %Y") %></p>
          <p class="card-text"><strong>Profile views:</strong> <%= @user.views %></p>
        </div>
      </div>

      <div class="mt-4">
        <h3>Posts by <%= @user.name %></h3>
        <% @user.posts.each do |post| %>
          <div class="card mb-3">
            <div class="card-body">
              <h5 class="card-title"><%= link_to post.title, post_path(post) %></h5>
              <p class="card-text"><small class="text-muted">Posted on <%= post.created_at.strftime("%B %d, %Y") %></small></p>
              <p class="card-text"><small class="text-muted"><%= pluralize(post.views, "view") %></small></p>
            </div>
          </div>
        <% end %>
      </div>

      <div class="mt-4">
        <h3>Comments by <%= @user.name %></h3>
        <% @user.comments.each do |comment| %>
          <div class="card mb-3">
            <div class="card-body">
              <p class="card-text"><%= comment.body %></p>
              <p class="card-text">
                <small class="text-muted">
                  On post: <%= link_to comment.post.title, post_path(comment.post) %>
                </small>
              </p>
              <p class="card-text">
                <small class="text-muted">
                  Posted on <%= comment.created_at.strftime("%B %d, %Y") %>
                </small>
              </p>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div> 